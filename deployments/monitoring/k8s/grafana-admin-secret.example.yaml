# Template only -- do NOT commit real credentials. Copy to
# grafana-admin-secret.yaml (gitignored), fill in a real password, apply
# before installing/upgrading the Helm release.
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
  namespace: monitoring
type: Opaque
stringData:
  admin-user: admin
  admin-password: "REPLACE_ME_generate_with_openssl_rand_-base64_24"
