# Minimal overrides; everything else uses apt-cacher-ng's packaged defaults.
CacheDir: /var/cache/apt-cacher-ng
LogDir: /var/log/apt-cacher-ng
Port: 3142
BindAddress: 0.0.0.0

# No auth, no TLS -- the 192 network is trusted, same as the rest of the mirror.
